# EXTERNAL RDS
# EXTERNAL MINIO
# EXTERNAL REDIS
# NO NGINX CONTROLLER

#postgresql:
#  install: false

registry:
  enabled: false
  ingress:
    enabled: false

nginx-ingress:
  enabled: false

certmanager:
  install: false

#redis:
#  install: false

prometheus:
  install: false

# Install separately with more configs
#gitlab-runner:
#  install: false

global:
  edition: ce
  hosts:
    domain: gitlab.dev-gitlab2.fences-bootstrap.p1.dev

  ## FIXME
#  psql:
#    host: 'CHANGEME'
#    password:
#      secret: psql-secret
#      key: psql-password
#    port: 5432
#    database: gitlab
#    username: gitlab

  ## FIXME -- creds
#  redis:
#    host: '$(REDIS_HOST)'
#    password:
#      enabled: true
#      secret: redis-secret
#      key: redis-password
#    port: 6379

#  minio:
#    enabled: false

#  appConfig:
#    lfs:
#      bucket: dev-gitlab2-fences-bootstrap-p1-k8s-gitlab-storage
#      connection:
#        secret: gitlab-credentials
#        key: accesskey
#    artifacts:
#      bucket: dev-gitlab2-fences-bootstrap-p1-k8s-gitlab-storage
#      connection:
#        secret: gitlab-credentials
#        key: accesskey
#    uploads:
#      bucket: dev-gitlab2-fences-bootstrap-p1-k8s-gitlab-storage
#      connection:
#        secret: gitlab-credentials
#        key: accesskey
#    packages:
#      bucket: dev-gitlab2-fences-bootstrap-p1-k8s-gitlab-storage
#      connection:
#        secret: gitlab-credentials
#        key: accesskey
#    backups:
#      bucket: dev-gitlab2-fences-bootstrap-p1-k8s-gitlab-storage
#      connection:
#        secret: gitlab-credentials
#        key: accesskey
#
  grafana:
    enabled: false

  imagePullPolicy: Always

  ingress:
    enabled: false
    configureCertmanager: false

gitlab:
  gitlab-shell:
    enabled: false

  unicorn:
    helmTests:
      enabled: false

    ingress:
      enabled: false

    unicorn:
      registry:
        enabled: false

  task-runner:
    backups:
      objectStorage:
        config:
          secret: s3cmd-config
          key: config

upgradeCheck:
  enabled: false
